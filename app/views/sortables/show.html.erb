<h1 class="lower"><%=  @sortable.title %></h1>
<br/>
<p>Here are the items in your list "<%=  @sortable.title %>" and these can actually be dragged and dropped in the same manner that your
participants will be dragging and dropping.  Go ahead and try it!</p>
<br/>
<ul id="sortables">
  <% for sortableitems in @sortableitems %>
  
  <% content_tag_for :li, sortableitems do %>
  <span class="handle">
    <%= sortableitems.description %>
  </span>
  <% end %>
  
  <% end %>

</ul>
<%= sortable_element("sortables", :url => sort_sortableitems_path, :handle => "handle") %>